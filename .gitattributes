# Normalise line endings. Shell scripts must stay LF or they break inside the
# Linux container ("bad interpreter"), even when committed from Windows.
* text=auto
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
